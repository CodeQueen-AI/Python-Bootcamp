# List Conversion : converting other data types into a list

# From string : each character becomes a list item
str_list = list("hello")
print(str_list) 

# From tuple : tuple elements become list items
tuple_list = list((1, 2, 3))
print(tuple_list)  
